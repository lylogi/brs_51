$('#view-comments-<%= @review.id %>').html('<%= j render @review.comments.order_by_time %>');
$('textarea.comment-area').val('');
$('.comment_edit').click(function(){
  $(this).text($(this).text() == '<%= t("layouts.cancel") %>' ?
    '<%= t"admin.book.edit_button" %>': '<%= t("layouts.cancel") %>');
  $(this.nextElementSibling).toggle();
});
